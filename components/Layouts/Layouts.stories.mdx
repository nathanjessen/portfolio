import { Canvas, Meta, Story } from '@storybook/addon-docs';
import { Container } from './Container';
import { DefaultLayout } from './DefaultLayout';

<Meta title='Layout' />

# Layout

## DefaultLayout

This component creates a default page layout. It includes a custom next/head with meta props to customize the document head for SEO optimization. It also includes layout components for navigation, main content, and the footer.

<Canvas>
  <DefaultLayout title='Custom Page Title | Site Name'>
    <div>Content</div>
  </DefaultLayout>
</Canvas>

## Container

The container component allows you to wrap content with a fixed width container with padding on the top, bottom, and sides.

<Canvas>
  <Container>
    <div>Content</div>
  </Container>
</Canvas>
